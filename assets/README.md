# Asset filenames

Upload these files into this `assets/` folder:

- `user-sohaum-mainali.jpg` — profile picture shown in the top-right user button.
- `favicon.ico` — site favicon used by the browser tab.
